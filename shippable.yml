language: go

go:
  - 1.7

build:
  pre_ci:
    - mkdir artifacts
    - jfrog rt dl builds/app.html artifacts/app.html

integrations:
  hub:
    - integrationName: mycompany-jfrog
      type: artifactory
