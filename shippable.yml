language: none
#
# go:
#   - 1.7

build:
  pre_ci:
    - jfrog rt dl pages/app.json

  pre_ci_boot:
    image_name: drydock/u14
    image_tag: tip
    pull: false
    options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'

  ci:
    - jfrog rt dl pages/app.html

integrations:
  hub:
    - integrationName: mycompany-jfrog
      type: artifactory
