resources:
  - name: app-package
    type: file
    integration: mycompany-jfrog
    pointer:
      sourceName: pages/data.json
    seed:
      versionName: initial

  - name: app-cluster
    type: cluster
    integration: mycompany-node-cluster
    pointer:
      sourceName: 'production-cluster'
